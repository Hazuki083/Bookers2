<main>
 <p id="notice"></p>
   <div class="container">
     <div class="row">
        <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
          <% if @user.errors.any? %>
           <%= @user.errors.count %>
            <% @user.errors.full_messages.each do |message| %>
             <%= message %>
             <% end %>
             <% end %>
          <h2>User info</h2>

          <%= form_with model: @user, local:true do |f| %>
        	<div class="form-group">
          <%= f.label :name, "Name" %>
          <%= f.text_field :name %>
          </div>

          <div class="form-group">
          <%= f.label :image, "Image" %>
          <%= f.attachment_field :profile_image %>

          <div class="form-group">
          <%= f.label :introduction, "Introduction" %>
          <%= f.text_field :introduction %>
          </div>
          <%= f.submit "Update User", class:"btn btn-secondary" %>
          <% end %>

          <!--users = ["taro", "jonh", "hanako"] // はいれつ-->
          <!--user.each do | user|-->
          <!-- <p>user</p>-->
          <!--end-->
          <!--user = {name: "taro", age: 21} // ハッシュ-->
          <!--users = [{name: "taro", age: 21}, {name: "john", age: 23}, {name: "hanako", age: 25}]-->
          <!--tag_name : ["soccer", "baseball", "tennis"]-->
          <!--<#% book.tag_name each do |tag|  %>-->
          <!-- tag-->
          <!--<#% end %>-->
        </div>
     </div>
   </div>
</main>